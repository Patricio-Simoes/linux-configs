############################
# Removes uuseful Packages #
############################

# These packages are installed via Flatpak.

sudo apt remove --purge firefox* -y
sudo apt remove --purge libreoffice* -y
